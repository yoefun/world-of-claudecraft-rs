[package]
name = "woc-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared intents, snapshots, and events for WoC-rs hosts"

[dependencies]
serde = { workspace = true }
